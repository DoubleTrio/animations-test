import { moderateScale } from './scale';

const FontSizes = {
  fontSizes: {
    icon: moderateScale(20),
    header: moderateScale(18),
    title: moderateScale(16),
    normal: moderateScale(12),
  },
};

export default FontSizes;